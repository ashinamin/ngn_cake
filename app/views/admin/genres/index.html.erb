<h3>ジャンル一覧・追加</h3>

<%= form_with model:@genre, url: admin_genres_path, method: :post do |f| %>

<%= f.label :"ジャンル名" %>
<%= f.text_field :name %>
<%= f.submit '新規登録', class: "btn btn-primary" %>

<% end %>

<table>
  <tr>
    
    
  </tr>
</table>